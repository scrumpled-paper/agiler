spring:
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

app:
  auth:
    tokenSecret: "dummysecretkey"
    refreshTokenSecret: "anotherdummysecretkey"
    token-expiry: 86400000
    refresh-token-expiry: 604800000
